# Дешёвый steering GPT-2 без деградации текста

> Черновик структуры. Маркеры `[RESULT]`, `[FIGURE]`, `[TABLE]`, `[HF]` и
> `[DECISION]` заменяются после финальных запусков.

## Краткий итог

Большой steering coefficient усиливает выбранное свойство, но разрушает качество
продолжения. Мы проверяем, может ли модель естественных активаций удалить
разрушительную часть вмешательства и сохранить нужное направление.

Основной baseline из задания: одношаговый MSE-denoiser. Мы сравниваем его с
наивным steering, GLP и `[DECISION: финальные дешёвые методы]` на одном наборе
prompts и одном диапазоне силы вмешательства.

Главный результат: `[RESULT: одна фраза о положении финального метода относительно
naive и GLP на Pareto-front]`.

Практический результат: `[RESULT: качество, property и стоимость inference лучшего
режима]`.

## 1. Постановка

Для активации \(h\in\mathbb{R}^{768}\) после шестого блока GPT-2 обычный steering
имеет вид

$$
h_{\text{steered}}=h+\alpha v.
$$

Рост \(\alpha\) усиливает свойство, но уводит модель от естественного режима работы.
Нас интересует весь компромисс между свойством и качеством, а не одна выбранная
точка.

Мы проверяем три вопроса.

1. Может ли простой MSE-denoiser из задания улучшить Pareto-front?
2. Даёт ли GLP лучший repair и оправдывает ли дополнительные шаги inference?
3. Можно ли сохранить полезную часть вмешательства более дешёвым способом?

## 2. Экспериментальный контракт

### 2.1 Модель и вмешательство

Используется GPT-2. Вмешательство применяется после `blocks.6.hook_resid_post`.
Prompt и специальные токены не меняются. Steering применяется только к новым
response tokens.

### 2.2 Направление

Два проверенных OpenAI SAE decoder directions не дали ожидаемого decoded effect.
Читающая и записывающая стороны этих features описывали разные свойства. Поэтому
основной эксперимент использует positive-sentiment DiffMean по SST-5.

Это отклонение от минимального SAE-примера задания. Оно фиксируется явно и не
выдаётся за SAE steering.

### 2.3 Данные denoiser

Естественные активации собираются на FineWeb. Train и validation разделяются по
hash документа. BOS исключается. Validation direction не используется при
обучении denoiser.

Mac-запуски проверяют обучение и отбирают методы. Финальные результаты считаются
на A100 с `[RESULT: объём train activations]` и `[RESULT: число seeds]`.

### 2.4 Генерация и метрики

Все методы используют одинаковые DExperts prompts, sampling settings, seeds и
полный \(\alpha\)-sweep.

Quality axis: conditional continuation NLL под чистой GPT-2. Perplexity является
его экспонентой. Dist-1, dist-2 и dist-3 показываются как дополнительные проверки
повторов, но не заменяют quality axis.

Property axis: вероятность положительной тональности продолжения по
SST-2-classifier. Полное распределение scores сохраняется. `[DECISION: добавить
или не добавлять независимую проверку свойства]`.

Сравнение проводится по Pareto-front. Точки с одинаковым \(\alpha\) не считаются
равными по достигнутому property.

## 3. Методы

### 3.1 Naive steering

Наивный baseline оставляет \(h+\alpha v\) без repair. Он показывает исходную цену
усиления свойства.

### 3.2 Additive MSE

Модель получает \(h+\varepsilon\) и восстанавливает \(h\). Проверяются простой MLP
из задания и сеть той же ширины и глубины, что GLP.

Capacity-matched вариант отделяет эффект objective от эффекта размера сети.

### 3.3 Interpolation MSE

Вход строится как интерполяция между естественной активацией и шумом. Сеть получает
уровень corruption и сразу предсказывает чистую активацию.

### 3.4 GLP

GLP учит velocity field на естественных активациях. Во время inference steered
state частично зашумляется и возвращается к данным несколькими Euler steps.

Проверяются несколько значений начального времени. Они задают силу repair и потерю
steering effect.

### 3.5 Дешёвые варианты

`[DECISION: перечислить только методы, прошедшие Mac-screening]`.

Для каждого варианта одной фразой указывается, что предсказывает сеть, сколько
вызовов нужно и чем objective отличается от GLP.

### 3.6 Геометрические варианты

Локальные tangent, normal и geodesic методы проверяют гипотезу о разделении
семантического и разрушительного движения. Эти величины используются только после
decoded causal ablation.

Safe correction удаляет из repair компоненту вдоль steering direction. Метод
проверяет, стирает ли denoiser само целевое свойство.

### 3.7 Не вошедшие прототипы

Curveball, UniSteer и INNSteer обсуждаются только как предварительные прототипы,
если их постановка не будет исправлена и повторена. Некорректный или насыщенный
прогон не считается отрицательным результатом статьи.

## 4. Результаты

### 4.1 Главный Pareto-front

`[FIGURE 1: property против NLL для naive, MSE, GLP и финального метода. Один цвет
на метод. Доверительные интервалы. Полный sweep остаётся в приложении.]`

Рисунок должен отвечать на один вопрос: какой метод даёт лучшее качество при одном
и том же достигнутом property?

`[RESULT: положение каждого финалиста относительно naive и GLP]`.

### 4.2 Численное сравнение

`[TABLE 1: method, matched property, NLL, perplexity, latency, число вызовов,
доверительный интервал]`.

Таблица содержит несколько заранее выбранных уровней property и интервалы
неопределённости. `[DECISION: правило статистического сравнения]`.

### 4.3 Что меняется внутри модели

`[FIGURE 2: один реальный prompt. Clean activation, \(h+\alpha v\), repair output и
correction. Отдельно показаны steering-coordinate и ортогональная correction.]`

Рисунок проверяет механизм, а не иллюстрирует PCA. Он показывает, сохраняет ли
repair нужное направление и какую часть вмешательства удаляет.

`[RESULT: causal interpretation для выбранного метода]`.

### 4.4 Decoded generations

`[FIGURE 3: три-четыре prompts. Строки содержат clean, naive, GLP и финальный
метод. Столбцы содержат слабый, средний и сильный property. Изменившиеся фрагменты
выделены.]`

Примеры выбираются заранее по типам случаев: типичный выигрыш, типичный проигрыш и
failure case. Они не выбираются только потому, что выглядят удачно.

`[RESULT: что именно ломается у naive и что исправляет или не исправляет repair]`.

### 4.5 Обучение

`[FIGURE 4: train и validation objective только для моделей, вошедших в финальное
сравнение. Отмечен выбранный checkpoint.]`

График подтверждает сходимость и выбор checkpoint. Он не используется как
доказательство качества steering.

### 4.6 Скорость

`[FIGURE 5: latency против качества при matched property. Подписи содержат число
вызовов denoiser.]`

`[RESULT: какой метод сохраняет преимущество после учёта inference cost]`.

### 4.7 Ablations

`[TABLE 2: simple/capacity MSE, corruption, repair strength, число flow steps,
safe correction и другие части только финального метода]`.

Каждая строка удаляет или заменяет одну часть метода. Строки с несколькими
одновременными изменениями не используются для причинного вывода.

## 5. Почему методы работают или не работают

Наивный steering напрямую записывает SST-5 sentiment direction. SST-2-evaluator
связан с ним по задаче и домену, поэтому этот baseline особенно силён. Это
совпадение учитывается при интерпретации результата.

Unconditional denoiser не знает, какая часть отклонения намеренна. Он может удалить
steering coordinate вместе с повреждением. `[RESULT: подтверждение или опровержение
по safe-correction ablation]`.

`[RESULT: механизм финального метода, подтверждённый Figure 2 и Table 2]`.

`[RESULT: failure case и граница применимости]`.

## 6. Ограничения

Основная property metric близка к данным, использованным для sentiment direction.
Она может переоценивать повторение положительной лексики.

GPT-2 меньше современных instruction-tuned LM. Выводы о геометрии и repair нельзя
автоматически переносить на другие модели.

`[RESULT: какие методы проверены только на Mac и какие получили финальный A100 run]`.

Исследование использует одно основное свойство. `[DECISION: нужна ли вторая
validation direction перед сдачей]`.

## 7. Воспроизводимость

Одна команда устанавливает зависимости. Одна команда обучает выбранные модели.
Одна команда воспроизводит evaluation и фигуры.

В репозитории фиксируются model revisions, data revisions, split, seeds,
конфигурации, checkpoint provenance и raw metrics.

Лучший checkpoint опубликован в `[HF]`. Его model card содержит архитектуру,
нормализацию, слой GPT-2, данные, лицензию и минимальный пример inference.

## 8. Вывод

`[RESULT: прямой ответ на вопрос задания в двух предложениях]`.

`[RESULT: что оказалось полезнее исходного MSE-denoiser и какой ценой]`.

`[RESULT: следующий эксперимент, который сильнее всего может изменить вывод]`.

## Ссылки

Список содержит первичные статьи для GLP, финального метода и каждой принятой
диагностики. Для каждой работы одной фразой указано, какую часть мы использовали и
что изменили в reduced-scale эксперименте.

`[REFERENCES: полные библиографические записи и ссылки]`.

## Приложение

В приложение входят полный Mac-screening, все \(\alpha\)-точки, дополнительные
метрики, rejected prototypes и ссылки на JSON, logs и HTML. Основные выводы отчёта
должны читаться без открытия приложения.
